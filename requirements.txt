# General Utilities
PyYAML>=6.0.1
pandas>=2.0.0

# Testing and Code Quality (used by CI)
flake8>=6.1.0
black>=23.11.0

# Note: PySpark is expected to be provided by the cluster environment.
# Do not list it here unless running in local mode without a cluster environment.
